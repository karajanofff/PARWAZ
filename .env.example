# Lokal ishlab chiqish
API_PORT=4000
JWT_SECRET=dev-ai-tekseriw-secret

# PostgreSQL (Render da avtomatik beriladi)
DATABASE_URL=postgresql://user:password@localhost:5432/parwaz
DATABASE_SSL=false

# Production (Render avtomatik o'rnatadi)
# NODE_ENV=production
# PORT — Render tomonidan beriladi
